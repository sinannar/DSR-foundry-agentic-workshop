# Facilitator Notes

- Confirm attendee project assignment list before start.
- Run `scripts/list-attendee-projects.py` after infra deployment.
- Keep one demo project as fallback for blocked attendees.
